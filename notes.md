🗂️ 2. Fitxer: docker-compose.yml (a l’arrel)
📌 Finalitat:

Coordina diversos contenidors (com PostgreSQL i Flask) en un sol fitxer de configuració, per poder aixecar-los amb una sola comanda:

docker-compose up --build